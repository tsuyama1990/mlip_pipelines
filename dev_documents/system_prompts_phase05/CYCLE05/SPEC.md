# CYCLE05 Specification

## Summary
This specification document meticulously details the absolute architectural requirements, stringent design constraints, and comprehensive implementation pathways specifically tailored for CYCLE05: Intelligent Cutout and Active Learning Loop of the PyAcemaker project. The paramount objective of this specific developmental phase is to systematically and incrementally construct a highly resilient, enterprise-grade architecture capable of supporting advanced Machine Learning Interatomic Potential generation. We are strictly adhering to the AC-CDD methodology, ensuring that every single feature is rigorously designed, meticulously tested, and perfectly integrated without introducing any form of brittle, tightly coupled logic. Fundamentally, for alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. Furthermore, any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. Consequently, by leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. Fundamentally, integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Therefore, this zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The orchestrator actively monitors the extrapolation grade.

## System Architecture
The underlying system architecture for Cycle 05 involves highly precise, surgical modifications and sophisticated extensions to the core computational engine. The overarching directory structure perfectly reflects a remarkably clean, strictly modular, and deeply hierarchical design paradigm. We will systematically create, carefully modify, and rigorously test specific files to integrate the novel features absolutely seamlessly into the existing framework. The architecture unequivocally guarantees a strict, impenetrable separation of concerns, ensuring that complex scientific domain logic remains entirely and perpetually isolated from transient, low-level infrastructure details, such as file system operations or specific hardware configurations. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. Furthermore, this robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Fundamentally, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. Consequently, we employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. In parallel, the Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Fundamentally, the intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. In parallel, when the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. As a direct result, if an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Fundamentally, any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. Consequently, a strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. Fundamentally, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to.

```text
.
├── src/
│   ├── core/
│   │   └── **orchestrator.py**
│   ├── domain_models/
│   │   └── **config.py**
│   └── dynamics/
│       └── **dynamics_engine.py**
```

The exact, non-negotiable code blueprints strictly dictate that all architectural modifications must be purely additive in nature. Under no circumstances will we engage in a destructive rewrite of the entire, highly stable legacy system. Instead, we will methodically extend the carefully validated existing classes and heavily tested functions using modern, elegant composition techniques.

## Design Architecture
The fundamental design architecture is entirely and resolutely driven by sophisticated, strictly typed Pydantic-based schemas. This critical development cycle officially introduces a suite of highly robust, deeply complex data models specifically engineered to accurately represent entirely new, nuanced domain concepts crucial for the active learning process. Key physical invariants—such as explicitly ensuring strict total energy conservation, validating correct complex physical dimensions (e.g., verifying forces are precisely in eV/Angstrom), and enforcing geometric boundaries—are relentlessly enforced through a matrix of strict, cascading validation rules embedded directly within the schema definitions. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. Crucially, during the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. Crucially, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. As a direct result, the Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. In parallel, this robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. As a direct result, any potential.

## Implementation Approach
The highly detailed, meticulously structured, step-by-step implementation guide for Cycle 05 emphatically demands a deeply methodical, rigorously test-driven development (TDD) approach. Cycle 05 represents the monumental integration phase where the 'Halt & Catch Fire' active learning loop is fully realised. The engine will parse real-time extrapolation grades ($\gamma$ values) and halt MD when thresholds are breached. The `Intelligent Cutout` logic will extract the highly uncertain epicentre, apply vacuum padding, and execute sophisticated auto-passivation algorithms to cap dangling bonds with fractional hydrogen atoms. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Fundamentally, by interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. Crucially, when the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. Furthermore, this robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. Consequently, if an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. Therefore, by interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. In parallel, we employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. Furthermore, the orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The Dynamics Engine acts as a.

## Test Strategy

### Unit Testing Approach
Testing focuses entirely on the geometric and physical correctness of the extraction algorithms. We will construct extremely complex mock atomic structures with deliberate defects. The tests will rigorously assert that the resulting extracted cluster contains exactly the correct core atoms, that buffer atoms have their force weights zeroed, and that auto-passivation correctly placed capping atoms at precise bond lengths. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. Therefore, the Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. Crucially, by interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. Fundamentally, for alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. Any potential.

### Integration Testing Approach
The comprehensive Integration Testing Approach specifically designed for Cycle 05 will rigorously and methodically verify the complex, highly nuanced interactions occurring specifically between the newly implemented computational components and the vast, existing legacy system framework. We will carefully simulate the entire, complex flow of heavy scientific data originating from the initial YAML configuration models, travelling safely through the central orchestrator, and finally arriving at the highly specific, isolated execution modules. Fundamentally, integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. In parallel, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. As a direct result, integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. A crucial component of the active learning loop is the.
