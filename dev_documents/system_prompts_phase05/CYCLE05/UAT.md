# CYCLE05 User Acceptance Testing

## Test Scenarios

Scenario ID: UAT-C05-01
Priority: Absolute Critical
Description: This highly sophisticated, fully immersive scenario rigorously evaluates the complete end-to-end user experience when specifically utilising the powerful, newly implemented scientific features of CYCLE05: Intelligent Cutout and Active Learning Loop. The comprehensive tutorial is meticulously designed to absolutely amaze and thoroughly educate users by explicitly demonstrating the flawless, seamless automation of incredibly complex atomic simulations. Through the elegant interface of a single, highly interactive Marimo notebook, the advanced user will explicitly define a highly complex, multi-component material interface, precisely specify highly advanced active learning hyper-parameters, and intimately witness the powerful system autonomously, flawlessly orchestrate the entire, massive computational workflow from absolutely zero. Fundamentally, the Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. As a direct result, the Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. In parallel, the Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. In parallel, the system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. Therefore, the system seamlessly transitions from standard molecular dynamics to.

## Behavior Definitions
GIVEN an advanced scientific user provides a perfectly valid, highly complex YAML configuration explicitly defining the target materials, extreme thermodynamic environments, and incredibly stringent desired accuracy metrics, WHEN the user explicitly initiates the powerful active learning computational pipeline directly through the interactive Marimo interface, THEN the highly autonomous system shall flawlessly, independently execute all necessary, complex structure generation algorithms, submit massive density functional theory calculations to the computing cluster, and flawlessly execute all sophisticated model training steps entirely without requiring any further, tedious manual intervention from the human operator.

GIVEN the massive, ongoing molecular dynamics simulation inevitably encounters an utterly unknown extrapolation region fraught with incredibly high systemic uncertainty, WHEN the previously defined, strict safety thresholds are dramatically and dangerously breached, THEN the highly intelligent system shall immediately, gracefully halt the powerful molecular dynamics engine to prevent catastrophic physical divergence. It will then meticulously, surgically extract the highly problematic, specific structural cluster, automatically perform advanced, highly localised quantum mechanical refinement calculations, completely update the underlying neural weights, and finally, seamlessly, flawlessly resume the paused simulation using the brand newly updated, highly robust potential. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. In parallel, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. This zero-shot distillation phase entirely bypasses expensive DFT.
