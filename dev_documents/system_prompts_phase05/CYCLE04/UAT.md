# CYCLE04 User Acceptance Testing

## Test Scenarios

Scenario ID: UAT-C04-01
Priority: Absolute Critical
Description: This highly sophisticated, fully immersive scenario rigorously evaluates the complete end-to-end user experience when specifically utilising the powerful, newly implemented scientific features of CYCLE04: ACE Trainer and Incremental Updates. The comprehensive tutorial is meticulously designed to absolutely amaze and thoroughly educate users by explicitly demonstrating the flawless, seamless automation of incredibly complex atomic simulations. Through the elegant interface of a single, highly interactive Marimo notebook, the advanced user will explicitly define a highly complex, multi-component material interface, precisely specify highly advanced active learning hyper-parameters, and intimately witness the powerful system autonomously, flawlessly orchestrate the entire, massive computational workflow from absolutely zero. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. Fundamentally, the pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. Fundamentally, if an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. As a direct result, when the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. In parallel, the system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC).

## Behavior Definitions
GIVEN an advanced scientific user provides a perfectly valid, highly complex YAML configuration explicitly defining the target materials, extreme thermodynamic environments, and incredibly stringent desired accuracy metrics, WHEN the user explicitly initiates the powerful active learning computational pipeline directly through the interactive Marimo interface, THEN the highly autonomous system shall flawlessly, independently execute all necessary, complex structure generation algorithms, submit massive density functional theory calculations to the computing cluster, and flawlessly execute all sophisticated model training steps entirely without requiring any further, tedious manual intervention from the human operator.

GIVEN the massive, ongoing molecular dynamics simulation inevitably encounters an utterly unknown extrapolation region fraught with incredibly high systemic uncertainty, WHEN the previously defined, strict safety thresholds are dramatically and dangerously breached, THEN the highly intelligent system shall immediately, gracefully halt the powerful molecular dynamics engine to prevent catastrophic physical divergence. It will then meticulously, surgically extract the highly problematic, specific structural cluster, automatically perform advanced, highly localised quantum mechanical refinement calculations, completely update the underlying neural weights, and finally, seamlessly, flawlessly resume the paused simulation using the brand newly updated, highly robust potential. Consequently, by defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Fundamentally, a strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. By interfacing with.
