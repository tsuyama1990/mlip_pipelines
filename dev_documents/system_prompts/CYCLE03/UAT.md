# CYCLE03 User Acceptance Testing

## Test Scenarios

Scenario ID: UAT-C03-01
Priority: Absolute Critical
Description: This highly sophisticated, fully immersive scenario rigorously evaluates the complete end-to-end user experience when specifically utilising the powerful, newly implemented scientific features of CYCLE03: The Self-Healing DFT Oracle. The comprehensive tutorial is meticulously designed to absolutely amaze and thoroughly educate users by explicitly demonstrating the flawless, seamless automation of incredibly complex atomic simulations. Through the elegant interface of a single, highly interactive Marimo notebook, the advanced user will explicitly define a highly complex, multi-component material interface, precisely specify highly advanced active learning hyper-parameters, and intimately witness the powerful system autonomously, flawlessly orchestrate the entire, massive computational workflow from absolutely zero. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. Crucially, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. The orchestrator actively monitors the extrapolation.

## Behavior Definitions
GIVEN an advanced scientific user provides a perfectly valid, highly complex YAML configuration explicitly defining the target materials, extreme thermodynamic environments, and incredibly stringent desired accuracy metrics, WHEN the user explicitly initiates the powerful active learning computational pipeline directly through the interactive Marimo interface, THEN the highly autonomous system shall flawlessly, independently execute all necessary, complex structure generation algorithms, submit massive density functional theory calculations to the computing cluster, and flawlessly execute all sophisticated model training steps entirely without requiring any further, tedious manual intervention from the human operator.

GIVEN the massive, ongoing molecular dynamics simulation inevitably encounters an utterly unknown extrapolation region fraught with incredibly high systemic uncertainty, WHEN the previously defined, strict safety thresholds are dramatically and dangerously breached, THEN the highly intelligent system shall immediately, gracefully halt the powerful molecular dynamics engine to prevent catastrophic physical divergence. It will then meticulously, surgically extract the highly problematic, specific structural cluster, automatically perform advanced, highly localised quantum mechanical refinement calculations, completely update the underlying neural weights, and finally, seamlessly, flawlessly resume the paused simulation using the brand newly updated, highly robust potential. In parallel, the Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. Consequently, we employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Fundamentally, the Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. Consequently, the Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. As a direct result, the Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. Consequently, this robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Consequently, the intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather.
