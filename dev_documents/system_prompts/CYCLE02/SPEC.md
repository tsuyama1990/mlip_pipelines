# CYCLE02 Specification

## Summary
This specification document meticulously details the absolute architectural requirements, stringent design constraints, and comprehensive implementation pathways specifically tailored for CYCLE02: Dynamics Engine and LAMMPS Integration of the PyAcemaker project. The paramount objective of this specific developmental phase is to systematically and incrementally construct a highly resilient, enterprise-grade architecture capable of supporting advanced Machine Learning Interatomic Potential generation. We are strictly adhering to the AC-CDD methodology, ensuring that every single feature is rigorously designed, meticulously tested, and perfectly integrated without introducing any form of brittle, tightly coupled logic. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. Therefore, during the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. The Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Therefore, the Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. The hierarchical distillation workflow begins by transferring.

## System Architecture
The underlying system architecture for Cycle 02 involves highly precise, surgical modifications and sophisticated extensions to the core computational engine. The overarching directory structure perfectly reflects a remarkably clean, strictly modular, and deeply hierarchical design paradigm. We will systematically create, carefully modify, and rigorously test specific files to integrate the novel features absolutely seamlessly into the existing framework. The architecture unequivocally guarantees a strict, impenetrable separation of concerns, ensuring that complex scientific domain logic remains entirely and perpetually isolated from transient, low-level infrastructure details, such as file system operations or specific hardware configurations. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. Consequently, the Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. Consequently, the system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. Therefore, this algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. In parallel, the orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. As a direct result, to prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. In parallel, to prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. Crucially, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. Furthermore, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Crucially, when the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. Consequently, the Structure Generator employs.

```text
.
├── src/
│   ├── core/
│   │   └── **orchestrator.py**
│   ├── domain_models/
│   │   └── **config.py**
│   └── dynamics/
│       └── **dynamics_engine.py**
```

The exact, non-negotiable code blueprints strictly dictate that all architectural modifications must be purely additive in nature. Under no circumstances will we engage in a destructive rewrite of the entire, highly stable legacy system. Instead, we will methodically extend the carefully validated existing classes and heavily tested functions using modern, elegant composition techniques.

## Design Architecture
The fundamental design architecture is entirely and resolutely driven by sophisticated, strictly typed Pydantic-based schemas. This critical development cycle officially introduces a suite of highly robust, deeply complex data models specifically engineered to accurately represent entirely new, nuanced domain concepts crucial for the active learning process. Key physical invariants—such as explicitly ensuring strict total energy conservation, validating correct complex physical dimensions (e.g., verifying forces are precisely in eV/Angstrom), and enforcing geometric boundaries—are relentlessly enforced through a matrix of strict, cascading validation rules embedded directly within the schema definitions. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. Fundamentally, during the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. In parallel, the Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. Therefore, this.

## Implementation Approach
The highly detailed, meticulously structured, step-by-step implementation guide for Cycle 02 emphatically demands a deeply methodical, rigorously test-driven development (TDD) approach. Cycle 02 focuses on the complex inter-process communication required to drive the external LAMMPS binary. The `DynamicsEngine` will be implemented to automatically generate highly specific `in.lammps` scripts, dynamically injecting the Lennard-Jones baseline potentials. We will implement the crucial log-parsing logic required to detect 'Lost Atoms' and other catastrophic physics failures in real-time. Consequently, the system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. The Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. This algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. Furthermore, by defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. Fundamentally, during the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. Crucially, we employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. Consequently, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. The pre-relaxation step utilises a foundational model like MACE-MP-0 to gently relax the buffer region whilst strictly freezing the core atoms. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. By defining rigorous minimum and maximum constraints on physical parameters, the Pydantic schemas prevent nonsense data from crashing the pipeline. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands.

## Test Strategy

### Unit Testing Approach
Integration tests will utilise a highly sophisticated `MockLAMMPS` class that perfectly simulates the standard output and error streams of the real C++ binary. We will verify that the Python engine correctly parses simulated crash logs and successfully extracts the final valid atomic snapshot before the failure occurred, all without running actual computationally expensive MD steps. The Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. Consequently, we employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. This robust checkpointing guarantees that if the High-Performance Computing cluster prematurely kills the job, the pipeline can resume instantly. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. Therefore, the entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. Furthermore, the orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The system seamlessly transitions from standard molecular dynamics to long-timescale Adaptive Kinetic Monte Carlo (aKMC) simulations. Furthermore, the Orchestrator operates as a stateful daemon, meticulously logging every data transaction and module invocation to an SQLite checkpoint database. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. When the gamma value breaches the predefined threshold,.

### Integration Testing Approach
The comprehensive Integration Testing Approach specifically designed for Cycle 02 will rigorously and methodically verify the complex, highly nuanced interactions occurring specifically between the newly implemented computational components and the vast, existing legacy system framework. We will carefully simulate the entire, complex flow of heavy scientific data originating from the initial YAML configuration models, travelling safely through the central orchestrator, and finally arriving at the highly specific, isolated execution modules. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. Integration tests specifically target the serialization and deserialization pathways between the Python orchestrator and the external C++ executables. For alloy systems, the policy heavily biases towards kinetic Monte Carlo moves to properly sample chemical short-range order. Crucially, to prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. As a direct result, the Oracle module automatically handles density functional theory calculations using Quantum Espresso, including complex self-healing routines. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. Consequently, to prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. As a direct result, any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. Fundamentally,.

## Post-Audit Refinements
The Dynamics Engine has been fully upgraded to eliminate OOM risks when interacting with extremely large datasets natively via reservoir sampling implementations using `ase.io.iread`. By integrating pure streaming techniques, the underlying system can process gigabytes of high-dimensional trajectory features without breaching container limitations. Security checks around the Pydantic boundaries strictly reject traversal and injection vectors. Test environments now execute natively on dynamic temporary directories preventing root escalation in shared paths. EON process wrappers construct fully isolated, valid configurations natively. No subprocess patching is left outside test-scope contexts, ensuring total integration transparency in staging.
