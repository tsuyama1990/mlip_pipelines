# CYCLE01 User Acceptance Testing

## Test Scenarios

Scenario ID: UAT-C01-01
Priority: Absolute Critical
Description: This highly sophisticated, fully immersive scenario rigorously evaluates the complete end-to-end user experience when specifically utilising the powerful, newly implemented scientific features of CYCLE01: Domain Models and Orchestrator Backbone. The comprehensive tutorial is meticulously designed to absolutely amaze and thoroughly educate users by explicitly demonstrating the flawless, seamless automation of incredibly complex atomic simulations. Through the elegant interface of a single, highly interactive Marimo notebook, the advanced user will explicitly define a highly complex, multi-component material interface, precisely specify highly advanced active learning hyper-parameters, and intimately witness the powerful system autonomously, flawlessly orchestrate the entire, massive computational workflow from absolutely zero. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. This zero-shot distillation phase entirely bypasses expensive DFT calculations, rapidly establishing a robust, physically plausible baseline potential. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. A crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. When the gamma value breaches the predefined threshold, the system triggers an automatic halt via the LAMMPS fix halt command. As a direct result, the Dynamics Engine acts as a robust wrapper around the LAMMPS C++ binary, capable of surviving process crashes via checkpoint restarts. The system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. Therefore, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out redundant structural data. The Structure Generator employs an Adaptive Exploration Policy, intelligently shifting between molecular dynamics and Monte Carlo swap steps. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. The orchestrator actively monitors the extrapolation grade (gamma value) emitted by the ACE potential at every simulation timestep. The test strategy.

## Behavior Definitions
GIVEN an advanced scientific user provides a perfectly valid, highly complex YAML configuration explicitly defining the target materials, extreme thermodynamic environments, and incredibly stringent desired accuracy metrics, WHEN the user explicitly initiates the powerful active learning computational pipeline directly through the interactive Marimo interface, THEN the highly autonomous system shall flawlessly, independently execute all necessary, complex structure generation algorithms, submit massive density functional theory calculations to the computing cluster, and flawlessly execute all sophisticated model training steps entirely without requiring any further, tedious manual intervention from the human operator.

GIVEN the massive, ongoing molecular dynamics simulation inevitably encounters an utterly unknown extrapolation region fraught with incredibly high systemic uncertainty, WHEN the previously defined, strict safety thresholds are dramatically and dangerously breached, THEN the highly intelligent system shall immediately, gracefully halt the powerful molecular dynamics engine to prevent catastrophic physical divergence. It will then meticulously, surgically extract the highly problematic, specific structural cluster, automatically perform advanced, highly localised quantum mechanical refinement calculations, completely update the underlying neural weights, and finally, seamlessly, flawlessly resume the paused simulation using the brand newly updated, highly robust potential. The Validator module acts as the final gatekeeper, automatically calculating phonon dispersion bands using the phonopy library. The intelligent cutout module isolates the highly uncertain epicentre of a halted simulation, carving out a spherical cluster of atoms. We employ the Python temporary directory module to strictly isolate file system operations, preventing tests from polluting the host environment. The test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. Fundamentally, the hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. During the training phase, the Pacemaker wrapper utilises incremental updates, starting from the previous potential's weights rather than learning from scratch. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. This pre-relaxation dramatically reduces unphysical strain and ensures the subsequent DFT calculations converge swiftly. By interfacing with the EON client, the engine can accurately explore slow diffusion pathways and calculate precise activation energy barriers. In parallel, the test strategy mandates absolutely zero side-effect execution; all external binaries and network calls must be exhaustively mocked. A strategically maintained replay buffer of historical structures is mixed with the newly acquired DFT data to prevent catastrophic forgetting. Therefore, the system enforces a rigorous Lennard-Jones baseline to prevent unphysical atomic collisions during extreme molecular dynamics simulations. By leveraging the Atomic Cluster Expansion (ACE) formalism, the potential captures high-order multi-body interactions with exceptional fidelity. To prevent artificial surface effects during DFT calculations, the excised cluster is subjected to automated hydrogen passivation. If an SCF convergence failure is detected, the Oracle automatically reduces the mixing beta and increases the electronic smearing temperature. Any potential that exhibits imaginary phonon frequencies at the Gamma point is automatically rejected and routed for further targeted learning. Furthermore, this algorithm mathematically maximises the determinant of the information matrix, ensuring only the most informative structures are sent to the Oracle. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. The hierarchical distillation workflow begins by transferring the vast generalisation capabilities of the MACE foundation model into the ACE framework. The entire architecture fundamentally relies on Pydantic models to define strict, type-safe data contracts between the asynchronous modules. Fundamentally, a crucial component of the active learning loop is the D-Optimality selection algorithm, which filters out.
